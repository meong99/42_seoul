#include "minishell.h"

char	*get_hererdocs_input(t_commands *commands)
{
	char	*str;
	char	*tmp;

	str = "";
	tmp = "";
	// while (ft_strncmp(tmp, commands->delimiter, \
	// 	ft_strlen(commands->delimiter)))
	// {
	// 	tmp = readline("> ");
	// 	str = ft_strjoin(str, tmp);
	// 	str = ft_strjoin(str, "\n");
	// }
	commands = 0;
	return (str);
}
